{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "watchPatterns": [
            "/apps/api/**"
        ]
    },
    "deploy": {
        "startCommand": "cd apps/api && celery -A celery_app worker --concurrency=1 --loglevel=info & cd apps/api && uvicorn main:app --host 0.0.0.0 --port $PORT",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}